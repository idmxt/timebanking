# ПЛАТФОРМА TIMEBANKING - ТЕХНИЧЕСКОЕ ЗАДАНИЕ И ПЛАН РАЗРАБОТКИ

## 🎯 ОБЗОР ПРОЕКТА

Разработать многопользовательскую веб-платформу для обмена навыками и услугами на основе времени (timebanking), где валютой является время, а не деньги. Один час помощи = один час получения помощи.

**Стек технологий:** React (frontend) + Node.js + Express (backend) + SQLite (database)

---

## 🎨 ТРЕБОВАНИЯ К ДИЗАЙНУ

### Уникальный Frontend (НЕ банальный ИИ-дизайн)

**ОБЯЗАТЕЛЬНЫЕ ПРИНЦИПЫ:**

1. **Цветовая схема**: Избегать стандартных градиентов синего/фиолетового
   - Использовать теплые земляные тона: терракота (#E07856), оливковый (#8B9D77), кремовый (#F5E6D3)
   - Акценты: янтарный (#D4A574) и глубокий зеленый (#2F5233)

2. **Типографика**: 
   - НЕ использовать Inter, Poppins или Roboto
   - Использовать: Outfit для заголовков + Manrope для текста
   - Или: Space Grotesk + Public Sans

3. **Визуальный стиль**:
   - Органичные формы вместо строгих прямоугольников
   - Мягкие тени: `box-shadow: 0 8px 24px rgba(0,0,0,0.08)`
   - Скругления: `border-radius: 16px` для карточек
   - Тонкие анимации при наведении (transform: translateY(-2px))

4. **Иконки**: 
   - Использовать lucide-react с stroke-width: 1.5
   - Добавить custom SVG иллюстрации для пустых состояний

5. **Компоненты**:
   - НЕ делать карточки с белым фоном и серой рамкой
   - Использовать цветные акценты и градиенты для категорий
   - Аватары пользователей с цветными рамками по статусу

---

## 📋 ОСНОВНОЙ ФУНКЦИОНАЛ

### 1. Система пользователей

**Регистрация и профиль:**
- Email + пароль (с bcrypt хешированием)
- Имя, фото профиля, город, краткое био
- Список навыков (что могу предложить)
- Список потребностей (что ищу)
- Баланс времени в часах (стартовый: 5 часов для новых)
- Рейтинг и отзывы

**Аутентификация:**
- JWT токены (access + refresh)
- Защищенные роуты через middleware
- Сессии с автоматическим обновлением токенов

### 2. Каталог услуг/навыков

**Категории:**
- Образование и репетиторство
- Ремонт и строительство
- IT и технологии
- Творчество и дизайн
- Кулинария и выпечка
- Консультации (юридические, финансовые, карьерные)
- Красота и здоровье
- Языковая практика
- Транспорт и логистика
- Помощь по дому
- Садоводство
- Животные (выгул, уход)

**Карточка услуги:**
- Название и подробное описание
- Категория с цветовым тегом
- Длительность (0.5ч, 1ч, 2ч, 3ч, custom)
- Локация (онлайн/оффлайн + адрес)
- Фото услуги (опционально)
- Доступность (расписание)
- Автор услуги с рейтингом

**Фильтры и поиск:**
- По категории
- По городу/району
- По доступности (сегодня, эта неделя, в любое время)
- По формату (онлайн/оффлайн)
- Полнотекстовый поиск

### 3. Система бронирования

**Создание запроса:**
1. Пользователь выбирает услугу
2. Видит профиль исполнителя и его рейтинг
3. Выбирает дату и время из доступных слотов
4. Пишет сообщение с деталями
5. Отправляет запрос

**Статусы запроса:**
- `pending` - ожидает ответа
- `accepted` - принято, время забронировано
- `declined` - отклонено
- `completed` - выполнено (требует подтверждения обеих сторон)
- `cancelled` - отменено

**Workflow:**
```
Запрос → Принятие → Выполнение → Подтверждение → Перевод времени + Отзыв
```

### 4. Система времени (Time Credits)

**Учет времени:**
- Каждый пользователь имеет баланс в часах
- Новые пользователи получают 5 часов стартового кредита
- При выполнении услуги: получатель -N часов, исполнитель +N часов
- Минимальный баланс: -10 часов (для активации обмена)
- История транзакций с датами и описаниями

**База данных транзакций:**
```sql
CREATE TABLE time_transactions (
  id INTEGER PRIMARY KEY,
  from_user_id INTEGER,
  to_user_id INTEGER,
  hours REAL,
  booking_id INTEGER,
  description TEXT,
  created_at DATETIME
)
```

### 5. Коммуникация

**Система сообщений:**
- Внутренний чат между пользователями
- Уведомления о новых сообщениях
- Контекст переписки привязан к конкретной услуге/бронированию
- Возможность отправки изображений
- Индикатор "печатает..."
- Статус прочтения сообщений

**Уведомления:**
- Новый запрос на услугу
- Принятие/отклонение запроса
- Напоминание за день до встречи
- Запрос на подтверждение выполнения
- Новое сообщение
- Новый отзыв

### 6. Отзывы и рейтинг

**После завершения:**
- Обе стороны оставляют отзыв (1-5 звезд)
- Текстовый комментарий
- Публичные отзывы на профиле
- Общий рейтинг как среднее всех оценок
- Бейдж "Надежный участник" после 10+ успешных обменов

---

## 🗄️ СТРУКТУРА БАЗЫ ДАННЫХ (SQLite)

```sql
-- Пользователи
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  email TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  name TEXT NOT NULL,
  avatar_url TEXT,
  city TEXT,
  bio TEXT,
  time_balance REAL DEFAULT 5.0,
  rating REAL DEFAULT 0,
  total_reviews INTEGER DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Навыки пользователя
CREATE TABLE user_skills (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  skill TEXT NOT NULL,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

-- Услуги
CREATE TABLE services (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  title TEXT NOT NULL,
  description TEXT,
  category TEXT NOT NULL,
  duration REAL NOT NULL, -- в часах
  location_type TEXT NOT NULL, -- 'online' или 'offline'
  address TEXT,
  image_url TEXT,
  is_active BOOLEAN DEFAULT 1,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

-- Доступность (расписание)
CREATE TABLE availability (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  service_id INTEGER NOT NULL,
  day_of_week INTEGER, -- 0-6 (Пн-Вс)
  start_time TEXT, -- 'HH:MM'
  end_time TEXT,
  FOREIGN KEY (service_id) REFERENCES services(id)
);

-- Бронирования
CREATE TABLE bookings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  service_id INTEGER NOT NULL,
  requester_id INTEGER NOT NULL,
  provider_id INTEGER NOT NULL,
  booking_date DATE NOT NULL,
  booking_time TIME NOT NULL,
  duration REAL NOT NULL,
  status TEXT DEFAULT 'pending', -- pending, accepted, declined, completed, cancelled
  message TEXT,
  requester_confirmed BOOLEAN DEFAULT 0,
  provider_confirmed BOOLEAN DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (service_id) REFERENCES services(id),
  FOREIGN KEY (requester_id) REFERENCES users(id),
  FOREIGN KEY (provider_id) REFERENCES users(id)
);

-- Транзакции времени
CREATE TABLE time_transactions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  from_user_id INTEGER,
  to_user_id INTEGER NOT NULL,
  hours REAL NOT NULL,
  booking_id INTEGER,
  transaction_type TEXT NOT NULL, -- 'service', 'bonus', 'refund'
  description TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (from_user_id) REFERENCES users(id),
  FOREIGN KEY (to_user_id) REFERENCES users(id),
  FOREIGN KEY (booking_id) REFERENCES bookings(id)
);

-- Сообщения
CREATE TABLE messages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  sender_id INTEGER NOT NULL,
  receiver_id INTEGER NOT NULL,
  booking_id INTEGER,
  content TEXT NOT NULL,
  is_read BOOLEAN DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (sender_id) REFERENCES users(id),
  FOREIGN KEY (receiver_id) REFERENCES users(id),
  FOREIGN KEY (booking_id) REFERENCES bookings(id)
);

-- Отзывы
CREATE TABLE reviews (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  booking_id INTEGER NOT NULL,
  reviewer_id INTEGER NOT NULL,
  reviewed_user_id INTEGER NOT NULL,
  rating INTEGER NOT NULL CHECK(rating >= 1 AND rating <= 5),
  comment TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (booking_id) REFERENCES bookings(id),
  FOREIGN KEY (reviewer_id) REFERENCES users(id),
  FOREIGN KEY (reviewed_user_id) REFERENCES users(id)
);

-- Уведомления
CREATE TABLE notifications (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  type TEXT NOT NULL, -- 'booking_request', 'booking_accepted', 'message', etc.
  title TEXT NOT NULL,
  message TEXT,
  link TEXT,
  is_read BOOLEAN DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

-- Индексы для производительности
CREATE INDEX idx_services_user ON services(user_id);
CREATE INDEX idx_services_category ON services(category);
CREATE INDEX idx_bookings_requester ON bookings(requester_id);
CREATE INDEX idx_bookings_provider ON bookings(provider_id);
CREATE INDEX idx_messages_receiver ON messages(receiver_id);
CREATE INDEX idx_notifications_user ON notifications(user_id);
```

---

## 🏗️ АРХИТЕКТУРА ПРИЛОЖЕНИЯ

### Frontend (React)

```
src/
├── components/
│   ├── layout/
│   │   ├── Header.jsx
│   │   ├── Footer.jsx
│   │   └── Sidebar.jsx
│   ├── auth/
│   │   ├── LoginForm.jsx
│   │   ├── RegisterForm.jsx
│   │   └── ProtectedRoute.jsx
│   ├── services/
│   │   ├── ServiceCard.jsx
│   │   ├── ServiceList.jsx
│   │   ├── ServiceDetail.jsx
│   │   ├── CreateServiceForm.jsx
│   │   └── FilterBar.jsx
│   ├── bookings/
│   │   ├── BookingForm.jsx
│   │   ├── BookingCard.jsx
│   │   └── BookingList.jsx
│   ├── profile/
│   │   ├── ProfileView.jsx
│   │   ├── ProfileEdit.jsx
│   │   ├── TimeBalance.jsx
│   │   └── ReviewsList.jsx
│   ├── messages/
│   │   ├── ChatWindow.jsx
│   │   ├── MessageList.jsx
│   │   └── ConversationItem.jsx
│   └── ui/
│       ├── Button.jsx
│       ├── Input.jsx
│       ├── Card.jsx
│       ├── Modal.jsx
│       ├── Badge.jsx
│       └── Avatar.jsx
├── pages/
│   ├── Home.jsx
│   ├── Services.jsx
│   ├── ServiceDetail.jsx
│   ├── CreateService.jsx
│   ├── Profile.jsx
│   ├── MyBookings.jsx
│   ├── Messages.jsx
│   └── Dashboard.jsx
├── hooks/
│   ├── useAuth.js
│   ├── useServices.js
│   ├── useBookings.js
│   └── useMessages.js
├── contexts/
│   ├── AuthContext.jsx
│   └── NotificationContext.jsx
├── utils/
│   ├── api.js
│   ├── formatters.js
│   └── validators.js
└── App.jsx
```

### Backend (Node.js + Express)

```
server/
├── config/
│   ├── database.js
│   └── constants.js
├── middleware/
│   ├── auth.js
│   ├── validation.js
│   └── errorHandler.js
├── routes/
│   ├── auth.routes.js
│   ├── users.routes.js
│   ├── services.routes.js
│   ├── bookings.routes.js
│   ├── messages.routes.js
│   ├── reviews.routes.js
│   └── notifications.routes.js
├── controllers/
│   ├── auth.controller.js
│   ├── users.controller.js
│   ├── services.controller.js
│   ├── bookings.controller.js
│   ├── messages.controller.js
│   ├── reviews.controller.js
│   └── notifications.controller.js
├── models/
│   ├── User.js
│   ├── Service.js
│   ├── Booking.js
│   ├── Message.js
│   └── Review.js
├── services/
│   ├── timeBank.service.js
│   ├── notification.service.js
│   └── email.service.js
├── utils/
│   ├── jwt.js
│   ├── validation.js
│   └── helpers.js
└── server.js
```

---

## 📝 ПОШАГОВЫЙ ПЛАН РАЗРАБОТКИ

### ЭТАП 1: Инициализация проекта (1-2 часа)

**Задачи:**
1. Создать структуру проекта с папками frontend и backend
2. Инициализировать npm проекты
3. Установить зависимости:

**Backend:**
```bash
npm init -y
npm install express sqlite3 bcryptjs jsonwebtoken cors dotenv express-validator
npm install --save-dev nodemon
```

**Frontend:**
```bash
npx create-react-app frontend
cd frontend
npm install react-router-dom axios lucide-react date-fns
```

4. Настроить базовую конфигурацию:
   - `.env` файл для переменных окружения
   - CORS настройки
   - Базовая структура Express сервера
   - SQLite database connection

**Результат:** Рабочая структура проекта с запускающимися dev серверами

---

### ЭТАП 2: База данных и модели (2-3 часа)

**Задачи:**
1. Создать файл `database/schema.sql` со всеми таблицами
2. Написать скрипт инициализации БД (`database/init.js`)
3. Создать модели для работы с БД:
   - User.js - методы для CRUD операций с пользователями
   - Service.js - управление услугами
   - Booking.js - работа с бронированиями
   - Message.js - сообщения
   - Review.js - отзывы

4. Добавить seed данные для тестирования:
   - 5-10 тестовых пользователей
   - 20-30 услуг в разных категориях
   - Несколько бронирований

**Результат:** Полностью настроенная БД с тестовыми данными

---

### ЭТАП 3: Аутентификация и авторизация (3-4 часа)

**Задачи:**
1. Создать auth controller с методами:
   - register (регистрация с хешированием пароля)
   - login (вход с генерацией JWT)
   - refresh (обновление токена)
   - logout

2. Middleware для проверки токенов (`auth.js`)
3. Frontend: AuthContext для глобального управления состоянием авторизации
4. Компоненты:
   - LoginForm с валидацией
   - RegisterForm с проверкой email
   - ProtectedRoute для защищенных страниц

5. Реализовать хранение токена в localStorage
6. Автоматический редирект при истечении сессии

**Результат:** Полностью работающая система регистрации/входа

---

### ЭТАП 4: Профили пользователей (2-3 часа)

**Задачи:**
1. Backend endpoints:
   - GET `/api/users/:id` - получить профиль
   - PUT `/api/users/:id` - обновить профиль
   - GET `/api/users/:id/reviews` - отзывы пользователя
   - GET `/api/users/:id/services` - услуги пользователя

2. Frontend компоненты:
   - ProfileView - просмотр профиля с рейтингом, балансом времени
   - ProfileEdit - редактирование своего профиля
   - TimeBalance - визуализация баланса времени (прогресс-бар)
   - SkillsBadges - список навыков с красивыми тегами

3. Загрузка аватара (сохранение в /uploads)

**Результат:** Система профилей с редактированием и просмотром

---

### ЭТАП 5: Каталог услуг (4-5 часов)

**Задачи:**
1. Backend endpoints:
   - GET `/api/services` - список с фильтрацией и пагинацией
   - GET `/api/services/:id` - детали услуги
   - POST `/api/services` - создать услугу
   - PUT `/api/services/:id` - обновить
   - DELETE `/api/services/:id` - удалить

2. Реализовать фильтры:
   - По категории (query param: `?category=IT`)
   - По городу (`?city=Almaty`)
   - По типу локации (`?location_type=online`)
   - Поиск (`?search=репетитор`)

3. Frontend компоненты:
   - ServiceList - грид карточек услуг (3 колонки на десктопе)
   - ServiceCard - карточка с изображением, категорией, автором, длительностью
   - FilterBar - боковая панель с фильтрами
   - CreateServiceForm - форма создания с выбором категории, расписания
   - ServiceDetail - страница услуги с кнопкой "Забронировать"

4. Красивый UI для категорий с иконками и цветами:
   - Образование: 📚 синий
   - IT: 💻 фиолетовый
   - Ремонт: 🔧 оранжевый
   - Творчество: 🎨 розовый
   - Кулинария: 🍳 желтый
   - и т.д.

**Результат:** Полнофункциональный каталог с поиском и фильтрами

---

### ЭТАП 6: Система бронирования (5-6 часов)

**Задачи:**
1. Backend endpoints:
   - POST `/api/bookings` - создать запрос на бронирование
   - GET `/api/bookings` - мои бронирования (как requester и provider)
   - PUT `/api/bookings/:id/accept` - принять запрос
   - PUT `/api/bookings/:id/decline` - отклонить
   - PUT `/api/bookings/:id/complete` - подтвердить выполнение
   - PUT `/api/bookings/:id/cancel` - отменить

2. Бизнес-логика:
   - Проверка доступности времени
   - Проверка баланса времени перед бронированием
   - Создание уведомлений при изменении статуса
   - Блокировка времени при принятии

3. Frontend компоненты:
   - BookingForm - выбор даты/времени (calendar picker)
   - BookingCard - карточка бронирования с статусом и действиями
   - BookingList - список с вкладками (Входящие/Исходящие/Завершенные)
   - StatusBadge - цветные бейджи для статусов

4. Timeline бронирования с визуальным индикатором прогресса

**Результат:** Полная система бронирования с управлением статусами

---

### ЭТАП 7: Система времени (Time Bank) (3-4 часа)

**Задачи:**
1. Backend service (`timeBank.service.js`):
   - `transferTime(fromUser, toUser, hours, bookingId)` - перевод времени
   - `addBonus(userId, hours, reason)` - бонусные часы
   - `refundTime(bookingId)` - возврат при отмене
   - `getTransactionHistory(userId)` - история транзакций

2. Автоматический перевод времени при подтверждении обеих сторон:
   ```javascript
   if (booking.requester_confirmed && booking.provider_confirmed) {
     await timeBankService.transferTime(
       booking.requester_id,
       booking.provider_id,
       booking.duration,
       booking.id
     );
     booking.status = 'completed';
   }
   ```

3. Frontend:
   - TimeBalance widget с анимированным счетчиком
   - TransactionHistory - таблица транзакций
   - Визуальные индикаторы +/- времени

4. Валидация минимального баланса (-10 часов)

**Результат:** Работающая экономика времени с автоматическими транзакциями

---

### ЭТАП 8: Система сообщений (4-5 часов)

**Задачи:**
1. Backend endpoints:
   - GET `/api/messages/:userId` - список бесед
   - GET `/api/messages/conversation/:otherUserId` - история с пользователем
   - POST `/api/messages` - отправить сообщение
   - PUT `/api/messages/:id/read` - пометить прочитанным

2. WebSocket или polling для real-time обновлений (начать с polling)

3. Frontend компоненты:
   - ChatWindow - окно чата с input и history
   - MessageList - список сообщений с группировкой по дате
   - ConversationItem - элемент списка бесед с превью
   - MessageBubble - пузырь сообщения (свое/чужое)

4. Badge с количеством непрочитанных в Header

**Результат:** Внутренний мессенджер для коммуникации

---

### ЭТАП 9: Отзывы и рейтинг (2-3 часа)

**Задачи:**
1. Backend endpoints:
   - POST `/api/reviews` - оставить отзыв
   - GET `/api/reviews/:userId` - отзывы о пользователе

2. Автоматический пересчет рейтинга:
   ```javascript
   const reviews = await Review.findByUserId(userId);
   const avgRating = reviews.reduce((sum, r) => sum + r.rating, 0) / reviews.length;
   await User.update(userId, { rating: avgRating, total_reviews: reviews.length });
   ```

3. Frontend:
   - ReviewForm - форма с звездами и текстом
   - ReviewCard - карточка отзыва с автором
   - StarRating - компонент звездочек (filled/empty)

4. Запрос на отзыв появляется после подтверждения обеих сторон

**Результат:** Система отзывов с автоматическим рейтингом

---

### ЭТАП 10: Уведомления (2-3 часа)

**Задачи:**
1. Notification service для создания уведомлений:
   ```javascript
   createNotification(userId, type, title, message, link)
   ```

2. Триггеры уведомлений:
   - Новое бронирование → провайдеру
   - Принятие/отклонение → реквестеру
   - Напоминание за 24 часа → обоим
   - Новое сообщение
   - Новый отзыв

3. Frontend:
   - NotificationBell в Header с badge
   - NotificationDropdown с списком
   - Кнопка "Отметить все прочитанными"

4. Polling для проверки новых уведомлений каждые 30 сек

**Результат:** Система уведомлений в реальном времени

---

### ЭТАП 11: Dashboard и аналитика (3-4 часа)

**Задачи:**
1. Backend endpoints:
   - GET `/api/dashboard/stats` - статистика пользователя

2. Метрики:
   - Баланс времени с графиком (последние 30 дней)
   - Количество завершенных обменов
   - Заработано/потрачено часов
   - Текущий рейтинг и прогресс до бейджа
   - Активные бронирования

3. Frontend компоненты:
   - StatsCard - карточка с метрикой
   - TimeChart - график баланса (recharts)
   - RecentActivity - лента активности
   - QuickActions - быстрые действия (создать услугу, найти услугу)

**Результат:** Информативный dashboard для пользователя

---

### ЭТАП 12: UI/UX полировка (4-5 часов)

**Задачи:**
1. Реализовать уникальный дизайн:
   - Применить цветовую схему везде
   - Добавить custom шрифты (Outfit + Manrope)
   - Органичные формы для карточек
   - Мягкие тени и скругления

2. Анимации и transitions:
   - Hover эффекты на карточках
   - Loading skeleton для загрузки
   - Smooth scroll
   - Page transitions (fade in/out)

3. Адаптивный дизайн:
   - Мобильная версия (320px+)
   - Tablet (768px+)
   - Desktop (1024px+)
   - Hamburger menu для мобилки

4. Иллюстрации для пустых состояний:
   - Нет услуг
   - Нет сообщений
   - Нет бронирований

5. Toast уведомления (react-hot-toast) для действий

**Результат:** Полированный, уникальный интерфейс

---

### ЭТАП 13: Дополнительные фичи (опционально, 3-4 часа)

**Задачи:**
1. **Избранное**: Сохранение услуг в избранное
2. **Рекомендации**: Алгоритм рекомендаций на основе истории
3. **Верификация**: Подтверждение email
4. **Социальный вход**: OAuth через Google
5. **Экспорт данных**: Скачать свою историю транзакций
6. **Групповые услуги**: Услуги для нескольких людей одновременно

**Результат:** Расширенный функционал платформы

---

### ЭТАП 14: Тестирование и отладка (3-4 часа)

**Задачи:**
1. Тестирование всех user flows:
   - Регистрация → создание услуги → получение запроса → выполнение → отзыв
   - Поиск услуги → бронирование → оплата временем → подтверждение

2. Тестирование edge cases:
   - Недостаточный баланс времени
   - Конфликт бронирований
   - Отмена после принятия
   - Удаление аккаунта с активными бронированиями

3. Проверка валидации:
   - Некорректные данные в формах
   - SQL injection защита
   - XSS защита

4. Performance:
   - Оптимизация запросов к БД (индексы)
   - Lazy loading изображений
   - Code splitting для React

5. Cross-browser тестирование (Chrome, Firefox, Safari)

**Результат:** Стабильное приложение без критических багов

---

### ЭТАП 15: Deployment и документация (2-3 часа)

**Задачи:**
1. Подготовка к продакшену:
   - Environment variables для секретов
   - Production build React (`npm run build`)
   - Nginx конфигурация для SPA
   - SQLite → PostgreSQL миграция (опционально)

2. Deployment:
   - Backend: Heroku, Railway, или VPS
   - Frontend: Vercel, Netlify, или тот же VPS
   - БД: файл SQLite или managed PostgreSQL

3. Документация:
   - README.md с инструкциями по установке
   - API документация (endpoints, примеры запросов)
   - User guide с скриншотами

4. Backup стратегия для БД

**Результат:** Развернутое приложение с документацией

---

## 🎯 КЛЮЧЕВЫЕ МОМЕНТЫ ДЛЯ ИИ АГЕНТА

### Приоритеты разработки

1. **Начать с базы**: БД и модели → Auth → Профили
2. **Core функционал**: Услуги → Бронирование → Timebank
3. **Коммуникация**: Сообщения → Уведомления
4. **Полировка**: UI → UX → Тестирование

### Частые ошибки которых избегать

❌ **НЕ ДЕЛАТЬ:**
- Использовать банальные цвета (синий градиент, фиолетовый)
- Стандартные шрифты (Inter, Roboto)
- Белые карточки с серой рамкой
- Игнорировать валидацию данных
- Забывать про мобильную версию
- Хранить пароли в plain text
- Не проверять баланс времени перед транзакцией

✅ **ОБЯЗАТЕЛЬНО:**
- Использовать уникальную цветовую палитру
- Валидировать все inputs
- Хешировать пароли bcrypt
- Защищать роуты JWT
- Делать responsive дизайн
- Добавлять loading states
- Обрабатывать ошибки gracefully

### Технические детали

**JWT токены:**
```javascript
// Access token: 15 минут
const accessToken = jwt.sign({ userId: user.id }, SECRET, { expiresIn: '15m' });
// Refresh token: 7 дней
const refreshToken = jwt.sign({ userId: user.id }, REFRESH_SECRET, { expiresIn: '7d' });
```

**Проверка баланса:**
```javascript
const canBook = async (userId, hours) => {
  const user = await User.findById(userId);
  return (user.time_balance - hours) >= -10; // Минимум -10
};
```

**Автоматические уведомления:**
```javascript
// При создании booking
await notificationService.create(
  providerId,
  'booking_request',
  'Новый запрос на услугу',
  `${requester.name} хочет забронировать "${service.title}"`,
  `/bookings/${booking.id}`
);
```

### Структура компонентов React

```jsx
// Пример уникального дизайна карточки
const ServiceCard = ({ service }) => {
  const categoryColors = {
    'IT': 'from-purple-400 to-purple-600',
    'Образование': 'from-blue-400 to-blue-600',
    'Ремонт': 'from-orange-400 to-orange-600'
  };

  return (
    <div className="group rounded-2xl overflow-hidden shadow-soft hover:shadow-lifted transition-all duration-300 hover:-translate-y-1">
      <div className={`h-2 bg-gradient-to-r ${categoryColors[service.category]}`} />
      <div className="p-6 bg-warm-cream">
        {/* Контент карточки */}
      </div>
    </div>
  );
};
```

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

**Многопользовательская платформа с:**

✅ Регистрация и профили пользователей  
✅ Каталог услуг с поиском и фильтрами  
✅ Система бронирования с подтверждениями  
✅ Экономика времени (timebanking)  
✅ Внутренний мессенджер  
✅ Отзывы и рейтинги  
✅ Уведомления в реальном времени  
✅ Личный dashboard с аналитикой  
✅ Уникальный, современный UI/UX  
✅ Адаптивный дизайн для всех устройств  
✅ Безопасная аутентификация  

**Технологии:**
- Frontend: React + React Router + Axios + Lucide React + Recharts
- Backend: Node.js + Express + SQLite + JWT + bcrypt
- Design: Custom цвета + Outfit/Manrope шрифты + Tailwind CSS

**Время разработки:** 35-50 часов чистого кодинга

---

## 🚀 БЫСТРЫЙ СТАРТ ДЛЯ ИИ АГЕНТА

**Команды для начала:**

```bash
# 1. Создать структуру
mkdir timebank-platform
cd timebank-platform
mkdir backend frontend

# 2. Backend setup
cd backend
npm init -y
npm install express sqlite3 bcryptjs jsonwebtoken cors dotenv express-validator
npm install --save-dev nodemon
mkdir config controllers routes models services middleware database utils

# 3. Frontend setup
cd ../frontend
npx create-react-app .
npm install react-router-dom axios lucide-react date-fns recharts
mkdir src/components src/pages src/hooks src/contexts src/utils

# 4. Создать .env файл
echo "PORT=5000
JWT_SECRET=your-super-secret-key-change-this
JWT_REFRESH_SECRET=your-refresh-secret-key
DATABASE_PATH=./database/timebank.db" > backend/.env

# 5. Запустить
# Terminal 1:
cd backend && npm run dev

# Terminal 2:
cd frontend && npm start
```

**Первый код для написания:**
1. `backend/server.js` - базовый Express сервер
2. `backend/database/schema.sql` - схема БД
3. `backend/database/init.js` - инициализация БД
4. `backend/config/database.js` - подключение к SQLite

---

## 💡 СОВЕТЫ ПО РЕАЛИЗАЦИИ

1. **Итеративная разработка**: Делать по одному этапу, тестировать перед переходом к следующему

2. **Commit часто**: После каждого завершенного этапа делать commit

3. **Логирование**: Добавить логи для отладки транзакций времени и статусов бронирований

4. **Seed данные**: Создать реалистичные тестовые данные для демонстрации

5. **Error handling**: Всегда возвращать понятные сообщения об ошибках

6. **Безопасность**: 
   - Sanitize все inputs
   - Используй prepared statements для SQL
   - Не логируй чувствительные данные
   - Rate limiting для API

7. **Performance**:
   - Индексы на часто запрашиваемые поля
   - Пагинация для списков
   - Lazy loading изображений
   - Debounce для поиска

---

## ✨ ЗАКЛЮЧЕНИЕ

Этот план покрывает все необходимые аспекты для создания полнофункциональной timebanking платформы. ИИ агент должен следовать этапам последовательно, уделяя особое внимание уникальности дизайна и качеству кода.

**Главные принципы:**
- Безопасность превыше всего
- UX должен быть интуитивным
- Дизайн должен быть уникальным
- Код должен быть читаемым и поддерживаемым

Успехов в разработке! 🚀